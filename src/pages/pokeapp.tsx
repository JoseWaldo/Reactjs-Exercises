import Layout from "../layouts/layout";
import PokeAppModule from "../modules/pokemon";

const PokeApp = () => {
  return (
    <Layout>
      <PokeAppModule />
    </Layout>
  );
};

export default PokeApp;
